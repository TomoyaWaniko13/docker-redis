# これは、新しいDockerイメージの基盤として使用する既存のイメージを指定しています。
# ここでは「Alpine Linux」というとても軽量なLinuxディストリビューションをベースとして使用しています。
# これは、新しいDockerイメージの基盤として使用する既存のイメージを指定しています。
FROM alpine

# この行は、イメージ内でコマンドを実行するための指示です。
# apkはAlpine Linuxのパッケージマネージャーです。
# このコマンドはRedisをダウンロードしてインストールしています。
RUN apk add --update redis

# これは、このイメージから作成されたコンテナが起動された時に実行されるコマンドを指定しています。
# この場合、コンテナが起動するとRedisサーバーが開始されます。
# これは、このイメージから作成されたコンテナが起動された時に実行されるコマンドを指定しています。
# この場合、コンテナが起動するとRedisサーバーが開始されます。
CMD ["redis-server"]